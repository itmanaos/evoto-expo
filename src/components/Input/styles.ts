import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: 56,
    width: '100%',
    borderRadius: 7,
    borderWidth: 2,
    padding: 16,
    fontSize: 16,
    backgroundColor: '#FFF',
    borderColor: '#2FDBBC',
    marginTop: 4,
    marginBottom: 4,
  },
});
